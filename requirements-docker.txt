# Requirements for Docker container-based Lambda
# Core Lambda and AWS dependencies
aws-lambda-powertools==2.32.0
boto3==1.34.34
slack-sdk==3.26.2

# HTTP dependencies (using only requests, removing aiohttp to avoid compilation issues)
requests==2.31.0
httpx==0.26.0

# Scraping dependencies
beautifulsoup4==4.12.3
lxml==5.1.0
playwright==1.41.2

# Stealth and automation
playwright-stealth==1.0.6

# Additional required dependencies (using versions with ARM64 wheels)
cachetools==5.5.0
pydantic==2.4.0
python-dateutil==2.8.2
